# ⚠️ 高风险点与Agent易犯错误规避指南

## 最容易踩坑的TOP 10风险点
### 1. 认证体系集成风险
**常见错误**:
- Authing回调地址配置错误，与微信开放平台白名单不匹配
- 混淆客户端/服务端环境变量，将Authing密钥暴露到前端
- JWT会话校验逻辑遗漏，API层没有二次权限校验
- 微信登录scope配置错误，无法获取用户openid/手机号
- 跨域配置错误导致登录回调失败

**规避方案**:
- 严格区分`NEXT_PUBLIC_`前缀的客户端变量和服务端私有变量
- 所有敏感配置全部走服务端环境变量，禁止在客户端组件中访问
- API层必须做权限二次校验，不能只依赖前端路由拦截
- 提前准备好已备案的域名，配置到微信开放平台和Authing白名单

### 2. 多数据库兼容风险
**常见错误**:
- 使用PostgreSQL特有字段类型（如JSONB、Array），导致SQLite不兼容
- 迁移脚本使用数据库特有语法，跨库执行失败
- 事务隔离级别配置不兼容，导致并发操作数据不一致
- 数据库连接池配置错误，生产环境连接耗尽

**规避方案**:
- Schema设计优先使用通用字段类型，特殊类型做兼容处理
- 迁移脚本必须同时在SQLite和PostgreSQL两种环境下测试通过
- 统一使用Prisma的查询API，禁止手写原生SQL
- 数据库层封装统一的Repository层，上层业务不直接操作Prisma Client

### 3. 权限控制漏洞风险
**常见错误**:
- 只做前端路由拦截，API接口没有权限校验，导致越权访问
- 用户等级判断逻辑错误，免费用户可以访问高级版功能
- 订阅过期后没有自动降级，用户可以继续使用付费功能
- 中间件匹配规则错误，部分接口绕过权限校验

**规避方案**:
- 所有私有API必须强制校验用户身份和权限等级
- 权限判断逻辑统一封装到auth工具库，全局复用
- 订阅状态每日定时校验，过期自动降级
- 中间件使用严格的路径匹配规则，覆盖所有私有路由

### 4. 支付系统安全风险
**常见错误**:
- Webhook接口没有做签名校验，导致伪造支付回调
- 支付回调没有幂等性处理，重复回调导致重复开通订阅
- 订阅生命周期逻辑错误，状态同步不及时
- 敏感支付数据记录到日志，导致信息泄露

**规避方案**:
- 所有支付回调必须严格校验平台签名，拒绝非法请求
- 回调接口必须实现幂等性，相同回调ID重复调用不产生副作用
- 支付相关操作全部记录审计日志，可追溯
- 禁止记录用户完整支付信息，敏感字段脱敏处理

### 5. CI/CD发布风险
**常见错误**:
- 数据库迁移没有事务包裹，迁移失败导致生产库损坏
- 构建顺序错误，先部署代码再执行迁移，导致线上报错
- 环境变量配置错误，生产环境使用测试环境配置
- 没有回滚机制，发布失败后无法快速恢复

**规避方案**:
- 迁移脚本必须在事务中执行，失败自动回滚
- 发布流程严格按照"执行迁移 → 部署代码 → 流量切量"顺序
- 生产环境变量全部通过GitHub Secrets管理，禁止硬编码
- 保留最近3个版本的镜像，支持一键快速回滚

### 6. UI/UX风格偏离风险
**常见错误**:
- 使用高饱和亮色，破坏对冲基金专业高雅的风格
- 动画效果过多过于花哨，显得不专业
- 排版层级混乱，留白不足，显得拥挤
- 响应式适配错误，大屏/小屏显示异常

**规避方案**:
- 严格使用预设的深蓝+金色系配色方案，禁止新增鲜艳颜色
- 动画只保留必要的微交互，时长控制在300ms以内
- 严格遵循设计规范的排版层级和留白标准
- 开发过程中同步测试1080p/2K/4K分辨率的显示效果

## 🔍 上线前必过Checklist
```
✅ 所有API接口权限校验测试通过
✅ 微信扫码登录全流程测试通过
✅ 支付流程与Webhook回调测试通过
✅ 订阅生命周期（开通/续费/升级/过期/取消）测试通过
✅ SQLite和PostgreSQL两种数据库兼容测试通过
✅ 三级用户权限隔离测试通过
✅ 敏感信息泄露扫描（密钥/支付信息不暴露）
✅ 生产环境构建无错误
✅ CI/CD流水线全流程测试通过
✅ 压力测试满足预期并发量
```

## 📈 后续优化建议
目前规划已覆盖核心流程，还可以根据业务发展补充：
1. 多语言国际化支持（如果服务海外客户）
2. 数据埋点与用户行为分析
3. 客服工单系统集成
4. 运营后台管理系统
5. 发票管理与财务对账功能
6. 多租户支持（如果后续扩展多业务线）